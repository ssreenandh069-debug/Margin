<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
        Exposes files in the app's internal cache directory (getCacheDir())
        under the "timetable_exports/" virtual prefix.
        Used by TimetableExportManager to create shareable content:// URIs.
    -->
    <cache-path name="timetable_exports" path="." />
</paths>
